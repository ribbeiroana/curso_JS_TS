const nomes = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Julia'];

// nomes.splice(indice, delete, elemento1, elemento2);
const removidos = nomes.splice(4, 1);
console.log(nomes, removidos);

// Number.MAX_VALUE = maximo de valor de um array